# MKRBOX Protocol

This folder holds the shared message schema for web <-> simulator communication.

- `mkrbox.schema.json` defines the MVP event types.
- Web and simulator adapters should validate messages against the schema.
